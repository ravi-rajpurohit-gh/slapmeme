# Meme Machine starter library

These short WAV stingers are original synthesized placeholders generated for this repository. They are grouped to demonstrate the app's built-in category browser and may be freely replaced with audio the user has rights to use.

The app copies missing files from this directory into its local app-data sound library on first launch. It never overwrites an existing local sound.
